package _import

// Switches the repo install, if it already exists,
// to a the newly imported repo and exports the current
// installtion to the exported location. Effectively,
// this switches the repo to the imported one config.
func swap(base string) (err error) {
	return
}
